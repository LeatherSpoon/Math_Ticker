/*
 * Local Three.js placeholder entrypoint.
 *
 * Production deployments should replace this file with a pinned vendored Three.js build
 * (for example from a trusted internal artifact mirror) to guarantee deterministic offline startup.
 */
